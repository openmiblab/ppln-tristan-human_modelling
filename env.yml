name: tristan
channels:
  - conda-forge
  - defaults
dependencies:
  # Core packages installed via Conda
  - python=3.10
  - joblib
  - pandas
  - numpy
  - matplotlib
  - pingouin
  - pip
  
  # Pip-only packages
  - pip:
    - -e .
    - -e C:/Users/md1spsx/Documents/GitHub/dcmri